---
title: Components
description: First-class code blocks, code tabs, and package-manager tabs.
---

<Spread image="/uploads/building-blocks.webp" alt="Floating rounded building blocks in warm terracotta tones" side="right" width="32%" top={4}>
Everything on this page is ordinary Markdown and MDX — the same components you
would reach for in any Fumadocs site, with no extra setup. What is different is
that every one of them stays editable in place: click *Edit* and the code
blocks keep their syntax highlighting, the tabs keep switching, and the tables
keep their alignment while you type into them.
</Spread>

## Code blocks

Fenced code blocks are syntax-highlighted (Shiki), with a title, a copy button,
and line highlighting — no setup required.

```ts title="lib/measure.ts" {3-4}
import { prepare, layout } from '@chenglou/pretext';

const prepared = prepare(text, '16px Inter');
const { lineCount, height } = layout(prepared, 640, 32);

console.log(lineCount, height);
```

## Code tabs

Group related snippets into tabs — great for showing the same thing in multiple
languages or frameworks.

<Tabs items={['TypeScript', 'JavaScript']}>

```ts tab="TypeScript"
export function greet(name: string): string {
  return `Hello, ${name}`;
}
```

```js tab="JavaScript"
export function greet(name) {
  return `Hello, ${name}`;
}
```

</Tabs>

## Package-manager tabs

A single `package-install` block becomes npm / pnpm / yarn / bun tabs, and the
manager you pick is remembered across the whole site.

```package-install
@chenglou/pretext
```

## Cards

<Cards>
  <Card title="Learn more about Next.js" href="https://nextjs.org/docs" />
  <Card title="Learn more about Fumadocs" href="https://fumadocs.dev" />
</Cards>

## Lists

Everything you expect from Markdown works out of the box:

- Bullet lists with **bold**, *italic*, and `inline code`
- Links like [pretext](https://github.com/chenglou/pretext) survive editing
- Every item is editable in place

1. Numbered lists too
2. With the same inline formatting
3. And the same in-place editing

## Quotes and dividers

> Documentation is a love letter that you write to your future self — and the
> reading experience is the envelope it arrives in.

---

## Tables

| Feature | docs.dev | Typical docs tool |
| --- | --- | --- |
| Text flows around figures | Yes — both sides | No |
| Edit the real page | Yes | Separate editor |
| Accessible flowed text | Yes | — |
